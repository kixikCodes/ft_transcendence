apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    options:
      # Grafana will read dashboard JSON files from this path inside the container.
      path: /etc/grafana/provisioning/dashboards/dashboards_files
