apiVersion: 1

# provisioned datasource: points to the Prometheus service defined in docker-compose
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    version: 1
